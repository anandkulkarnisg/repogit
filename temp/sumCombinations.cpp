#include<iostream>
#include<vector>
#include<string>

using namespace std;

int countWays(vector<int> vec, const int& n, const std::size_t size& idx)
{







}

int main(int argc, char* argv[])
{
	int n = 9;
	vector<int> vec = { 2, 3, 4, 5 };
	cout << countWays(vec, n, 0) << endl;
	return(0);
}
